#开发环境配置文件
spring:
  application:
    name: dioxide-service-two

server:
  port: 8763
eureka:
  instance:
    prefer-ip-address: true
    lease-renewal-interval-in-seconds: 5
    lease-expiration-duration-in-seconds: 20
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
